[package]
name = "obs-preview"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
anyhow = "1.0.86"
env_logger = "0.11.5"
libobs-wrapper = { version = "4.0.0", path = "../../libobs-wrapper", features = ["blocking"] }
libobs-sources = { version = "2.0.6", path = "../../libobs-sources", features = ["blocking"] }
winit = "0.30.9"
